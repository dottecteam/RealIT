# App
Onde todo o código fonte do projeto frontend web se encontra.

> Dentro do **Next.js**, todos os diretórios que possuem um arquivo ```page.tsx``` são considerados rotas de url.

> Os diretórios com parênteses (()) são "ignorados" na url.